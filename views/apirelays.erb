{ "relays" : [      <% @relays.each do |relay| %>
   {       <% rstatus = getrelaystatus(relay.id) %>
      "id" : <%= relay.id %>,
      "name" : "<%= relay.name %> " ,
      "localname" : "<%= relay.localname %> " ,
      "place" : "<%= relay.place %> " ,
      "masterip" : "<%= relay.masterip %> " ,
      "description" : "<%= relay.description %> " ,
      "status" : "<%= rstatus %> " 
<% if relay.equal?(@relays.last) %>      }
<% else %>      },<%end%>
      <% end %>] }
